fun main() {
    println("subhan")                             //always run
    println("subhan" + " " + "bashir")
    println(1 + 2)

// variables

    var score = 5             //var can reassign
    val score1 = 10           // val cannot reassign
    println(score)
    println(score1)

    var score3 : Int = 12
    var name : String = "subhan"
    var lie : Boolean = false
    var alpa : Char ='A'
    println(score3)

//arithmetic operator +,- *, / ,%
    println(score3.toFloat() / score)  //change the variable type
    println(10 % 4)
    //increment and decrement operator i++ , i-- , --i, ++i post and pre increment

    // visible modifier
    // private can be accessable everywhere
    // internal only in the module or with i project
    // public can be accesseble with in the file or within the class
    // protected can be accessable within the classes

}

